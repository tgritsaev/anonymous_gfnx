seed: 111
env_init_seed: 1
eval_init_seed: 2

num_train_steps: 62500
num_envs: 16

agent:
  learning_rate: 1e-3
  logZ_learning_rate: 0.1 # New field
  start_eps: 0.1
  train_backward: true

environment:
  reward: "easy"
  dim: 4
  side: 20

metrics:
  n_rounds : 10